server:
  port: 8080

---

spring:
  config:
    activate:
      on-profile: dev

logging:
  config: classpath:logback-dev.xml

---

spring:
  config:
    activate:
      on-profile: prod

logging:
  config: classpath:logback.xml
